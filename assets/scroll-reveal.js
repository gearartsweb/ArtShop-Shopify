/*
 * DEPRECATED — superseded by the inline IntersectionObserver in
 * layout/theme.liquid (script block #1). The selectors and handlers
 * that lived here (.story-row, button[name="plus"|"minus"]) are
 * unused by the current theme.
 *
 * File kept as an empty stub so any cached asset_url reference
 * resolves without 404.
 */
