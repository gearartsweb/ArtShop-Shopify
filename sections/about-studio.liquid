<div class="about-studio-container" style="background: {{ section.settings.bg_color }}; padding: 80px 0;">
  <div class="page-width">
    <div class="about-grid">
      
      <div class="about-visual-sticky">
        {% if section.settings.image != blank %}
          <img 
            src="{{ section.settings.image | image_url: width: 1000 }}" 
            alt="Fine Art Supplies Guide"
            loading="lazy"
            width="{{ section.settings.image.width }}"
            height="{{ section.settings.image.height }}"
            class="about-main-img"
          >
        {% else %}
          {{ 'lifestyle-1' | placeholder_svg_tag: 'placeholder-svg shadow' }}
        {% endif %}
        <div class="floating-badge">Est. 2006</div>
      </div>

      <div class="about-text-content">
        <span class="about-eyebrow">{{ section.settings.eyebrow | default: "Since 2006" }}</span>
        <h2 class="about-heading">{{ section.settings.title | default: "The Art Shop Hermanus" }}</h2>
        
        <div class="about-description">
          <p>Since 2006, The Art Shop Hermanus has served as a cornerstone of the Western Cape’s creative community. What began as a local retail space has grown into a trusted hub for artists, crafters, and visionaries across the region.</p>
          <p>We specialize in high-end fine art supplies, focusing on a curated selection of both local and imported products. Our philosophy is simple: premium quality shouldn’t come with a premium barrier. We stretch our margins to ensure that the best tools remain affordable for every creator.</p>
          <p>When you walk through our doors, you aren’t just a customer—you’re a peer. Our promise is a professional, helpful experience defined by a warm smile and a commitment to your craft. If we don’t have what you need on the floor, we’ll go above and beyond to find it for you.</p>
        </div>

        <div class="about-features-grid">
          <div class="feature-item">
            <strong>Premium & Affordable</strong>
            <p>Stretched margins to ensure top-tier tools fit your budget.</p>
          </div>
          <div class="feature-item">
            <strong>Artist-to-Artist</strong>
            <p>A professional, helpful experience from peers who value your craft.</p>
          </div>
        </div>
      </div>

    </div>
  </div>
</div>

<style>
  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: start;
  }
  .about-visual-sticky {
    position: sticky;
    top: 100px;
  }
  .about-main-img {
    width: 100%;
    height: auto;
    border-radius: 2px;
    filter: grayscale(20%); /* Elegant studio look */
  }
  .about-eyebrow {
    text-transform: uppercase;
    letter-spacing: 2px;
    font-size: 11px;
    color: #888;
    display: block;
    margin-bottom: 15px;
  }
  .about-heading {
    font-size: clamp(24px, 4vw, 36px);
    margin: 0 0 30px 0;
    line-height: 1.2;
  }
  .about-description {
    font-size: 16px;
    line-height: 1.8;
    color: #333;
    margin-bottom: 40px;
  }
  .about-description p {
    margin-bottom: 1.5em;
  }
  .about-features-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    border-top: 1px solid #eee;
    padding-top: 30px;
  }
  .feature-item strong {
    display: block;
    font-size: 13px;
    text-transform: uppercase;
    margin-bottom: 5px;
  }
  .feature-item p {
    font-size: 13px;
    color: #777;
    margin: 0;
  }
  .floating-badge {
    position: absolute;
    bottom: -20px;
    right: -20px;
    background: #800000;
    color: #fff;
    padding: 20px;
    font-size: 12px;
    letter-spacing: 1px;
    transform: rotate(-5deg);
  }

  @media screen and (max-width: 749px) {
    .about-grid { grid-template-columns: 1fr; gap: 40px; }
    .about-visual-sticky { position: relative; top: 0; }
    .floating-badge { right: 10px; }
  }
</style>

{% schema %}
{
  "name": "About Studio (Editorial)",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Small Top Label" },
    { "type": "text", "id": "title", "label": "Heading", "default": "The Art Shop Hermanus" },
    { "type": "richtext", "id": "text", "label": "Description" },
    { "type": "image_picker", "id": "image", "label": "Studio Image" },
    { "type": "color", "id": "bg_color", "label": "Background Color", "default": "#ffffff" }
  ],
  "presets": [{ "name": "About Studio (Editorial)" }]
}
{% endschema %}